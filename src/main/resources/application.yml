server:
  servlet:
    context-path: /auth
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Auth
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL81Dialect
